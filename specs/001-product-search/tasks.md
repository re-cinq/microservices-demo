---

description: "Task list for the product-search feature (001-product-search). Generated by /speckit-tasks from spec.md + plan.md + research.md + data-model.md + contracts/ + quickstart.md."
---

# Tasks: Product Search

**Input**: Design documents from `/specs/001-product-search/`
**Prerequisites**: `plan.md` ✓, `spec.md` ✓, `research.md` ✓, `data-model.md` ✓, `contracts/searchproducts.rpc.md` ✓, `contracts/search-route.http.md` ✓, `quickstart.md` ✓

**Tests included?** One backend unit test is included (mandated by `contracts/searchproducts.rpc.md` — "Test obligations introduced by this contract"). No frontend handler tests are added; the codebase has no precedent for them and the feature spec does not ask for TDD across the whole stack.

**Organization**: Tasks are grouped by user story. Each phase ends in a checkpoint that delivers independently testable value (US1 = shoppers can search from the home page; US2 = empty/oversize/no-match queries are graceful; US3 = search reachable from every page).

## Implementation Notes (added during /speckit-implement)

The implementation merged a handful of tasks into combined edits to avoid redundant write cycles. Each task is marked `[X]` if the work landed (regardless of which file it landed in); the file-level location is documented inline. Tasks marked `[ ]` (deferred) require an environment the implementer didn't have at execute time — either a running Kubernetes cluster or a local Go toolchain. They're listed for whoever picks the branch up next.

**Merge map:**

- T009 (search form in `home.html`) and T014 (move form to `header.html`) were collapsed into a single edit that adds the form directly to `header.html`. Same end state, one fewer churn cycle.
- T007 (searchHandler), T011 (empty/oversize validation), and T016 (log line) were written as a single handler with validation and structured logging from the start. Same end state.
- T006 (results template — matches branch only) and T012 (template — no-match + empty-query branches) were written as a single template with all three branches in place.

**Deferred (require deploy or local Go):**

- T010, T013, T015, T017, T018 — quickstart smoke tests; require the storefront deployed to a cluster.
- T019 — `go test ./...`; no Go on this machine. CI will exercise it on push to `attendee/tim-love`.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Maps the task to a spec user story. Setup, Foundational, and Polish tasks have no story label.
- Every task description includes the exact file path it touches.

## Path Conventions

Existing monorepo. Frontend code lives under `src/frontend/`; product catalog service under `src/productcatalogservice/`. No new top-level directories are created.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Establish a clean baseline before any code change lands.

- [X] T001 Verify clean working tree on branch `001-product-search` and that baseline tests pass: run `git status` (expect: only spec/plan/research/contracts/quickstart/checklist/tasks files modified or untracked under `specs/001-product-search/` and the existing `CLAUDE.md` / `.specify/` / `.claude/` artefacts) and `cd src/productcatalogservice && go test ./...` (expect: all green, including the existing `TestSearchProducts`).

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Confirm the structural preconditions for the feature are already in place. **No code change required** — this phase exists to make the prerequisite explicit and to catch any environmental surprises before story work starts.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

- [X] T002 Confirm the frontend already has a wired gRPC connection to `productcatalogservice` that the new client wrapper will reuse: read `src/frontend/main.go` lines 132 and 142 (env var binding for `PRODUCT_CATALOG_SERVICE_ADDR` + `mustConnGRPC` for `productCatalogSvcConn`) and `src/frontend/rpc.go` lines 45-49 (`getProducts` uses the same connection). No code edit. Document the reuse decision in your task notes if you keep them.

**Checkpoint**: Foundation verified — user story implementation can begin.

---

## Phase 3: User Story 1 — Find product by name (Priority: P1) 🎯 MVP

**Goal**: A shopper visiting the home page can type a substring of a product's name into a search input, submit, and reach a results page that lists each matching product with picture, name, and localised price. Each result links to the existing `/product/{id}` detail page.

**Independent Test**: Per `quickstart.md` §1 — search `watch` from the home page, expect the Watch product to appear with a working detail-page link.

### Tests for User Story 1

> Write this test FIRST and confirm it FAILS against the current implementation (which matches name OR description). Then T004 narrows the predicate and the test passes.

- [X] T003 [US1] Add a name-only assertion test to `src/productcatalogservice/product_catalog_test.go`: extend the fixture in `TestMain` with one extra product whose `Name` does NOT contain the substring `"beta"` but whose `Description` does (e.g. `{Id: "abc005", Name: "Product Epsilon", Description: "matches beta in description only"}`), then add `TestSearchProductsNameOnly` asserting `SearchProducts("beta")` returns exactly 0 results. Run `go test ./...` — confirm the new test FAILS at this point.

### Implementation for User Story 1

- [X] T004 [US1] Narrow `SearchProducts` matching predicate to name-only in `src/productcatalogservice/product_catalog.go`: at lines 65-66, drop the description-substring branch so the predicate is `if strings.Contains(strings.ToLower(product.Name), strings.ToLower(req.Query))` only. Run `go test ./...` — confirm both `TestSearchProducts` (existing) and `TestSearchProductsNameOnly` (from T003) now PASS.

- [X] T005 [P] [US1] Add `searchProducts(ctx, query)` gRPC client wrapper to `src/frontend/rpc.go`, modeled on `getProducts` at lines 45-49. Signature: `func (fe *frontendServer) searchProducts(ctx context.Context, query string) ([]*pb.Product, error)`. Body: `resp, err := pb.NewProductCatalogServiceClient(fe.productCatalogSvcConn).SearchProducts(ctx, &pb.SearchProductsRequest{Query: query}); return resp.GetResults(), err`.

- [X] T006 [P] [US1] Create `src/frontend/templates/search-results.html` with a `{{ define "search-results" }}` block. Mirror the product-grid section from `src/frontend/templates/home.html`: for the matches branch, render each `{{ range .items }}` item as a card with `<img src="{{ .Item.Picture }}">`, `{{ .Item.Name }}`, `{{ renderMoney .Price }}`, and a link `href="{{ $.baseUrl }}/product/{{ .Item.Id }}"`. Include `{{ template "header" . }}` and `{{ template "footer" . }}` so the page inherits the existing chrome. Leave the no-matches and empty-query branches as `TODO` comments — T012 fills them in for US2.

- [X] T007 [US1] Implement `searchHandler` in `src/frontend/handlers.go`, modeled on `homeHandler` at lines 59-120. Behaviour: read `q` from `r.URL.Query().Get("q")`, trim, build the `[]searchResultView` (struct mirroring the local `productView` in `homeHandler`) by calling `fe.searchProducts(r.Context(), trimmed)` and per-result `fe.convertCurrency(r.Context(), p.GetPriceUsd(), currentCurrency(r))`, also fetch `fe.getCurrencies(ctx)` and `fe.getCart(ctx, sessionID(r))` so the header renders correctly, then `templates.ExecuteTemplate(w, "search-results", injectCommonTemplateData(r, map[string]interface{}{...}))` passing `query`, `items`, `no_matches: len(items)==0`, `show_currency: true`, `currencies`, `cart_size`. For US1 scope, do **not** handle empty/oversize queries — those land in US2.

- [X] T008 [US1] Register the GET route in `src/frontend/main.go` alongside the existing routes around line 150: `r.HandleFunc(baseUrl+"/search", svc.searchHandler).Methods(http.MethodGet, http.MethodHead)`. No other changes to `main.go`.

- [X] T009 [P] [US1] Add the search input form to `src/frontend/templates/home.html` (inside the `{{ define "home" }}` block, at the top of the products section). Markup: `<form method="GET" action="{{ $.baseUrl }}/search" class="search-form"><input name="q" type="text" required maxlength="200" placeholder="Search products"><button type="submit">Search</button></form>`. (US3 moves this form into the global header; for the US1 MVP it lives on the home page only.)

- [ ] T010 [US1] Smoke-test US1 per `specs/001-product-search/quickstart.md` §1: rebuild and redeploy `frontend` + `productcatalogservice` (the CI auto-deploys on push to `attendee/<your-name>`), open the storefront, search `watch` from the home page, confirm the Watch product appears with picture/name/price and that clicking it lands on `/product/{id}`. Repeat with `sun`, `loaf`, `dryer`.

**Checkpoint**: User Story 1 is fully functional. Shoppers can search from the home page; results render with correct currency; happy-path acceptance scenarios in spec User Story 1 are met. Empty/no-match handling is still ugly — that's US2.

---

## Phase 4: User Story 2 — Empty / no-match / oversize handling (Priority: P2)

**Goal**: Empty, whitespace-only, no-match, and oversize queries render explanatory pages, never the full catalog and never a 5xx.

**Independent Test**: Per `quickstart.md` §4 — submit `zzzzz`, empty, whitespace-only, `!!!!!`, a 201-char string, and `あ`; confirm each renders gracefully with the right HTTP code.

> US2 builds on US1's handler and template (it extends both). Each story still delivers independently demonstrable value, but they share files. Do not merge US2 commits before US1 commits.

- [X] T011 [US2] Extend `searchHandler` in `src/frontend/handlers.go`: after trimming the query string, branch as follows. If trimmed length is 0 → execute the `"search-results"` template with `query: ""`, `no_matches: true`, `items: nil` (HTTP 200). If trimmed length > 200 → call `renderHTTPError(log, r, w, errors.New("search query too long (max 200 characters)"), http.StatusBadRequest)` and return. Otherwise → continue to the existing US1 implementation.

- [X] T012 [US2] Extend `src/frontend/templates/search-results.html` with two new template branches. (a) When `{{ if eq .query "" }}`: render a "Please enter a search term" panel with a link back to `{{ $.baseUrl }}/`. (b) When `{{ if .no_matches }}` and `query` is non-empty: render `No matches for "{{ .query }}"` plus the same link back to home. Leave the matches-grid branch (added in T006) intact.

- [ ] T013 [US2] Smoke-test US2 per `specs/001-product-search/quickstart.md` §4: confirm `zzzzz` → no-matches page (200); empty `q` → empty-query page (200); whitespace-only `q` → empty-query page (200); `!!!!!` → no-matches page (200); 201-char string → query-too-long error (400); `あ` → no-matches page (200). No 5xx in any case.

**Checkpoint**: User Stories 1 + 2 both functional. The feature is production-credible: happy path works, sad paths are graceful. Search still only reachable from the home page.

---

## Phase 5: User Story 3 — Search reachable from every page (Priority: P3)

**Goal**: A shopper who navigated away from the home page (to a cart page, to a product detail page) can re-search without going back, because the search input lives in the global header.

**Independent Test**: Per `quickstart.md` §5 — submit a search from `/cart` and from `/product/{id}`; confirm the same results page renders as a home-page-initiated search.

> US3 reorganises the search input added by US1. Independently testable, but structurally a refactor of US1's template work.

- [X] T014 [US3] Move the search form: delete the `<form>` block added to `src/frontend/templates/home.html` by T009, and paste it into the navbar inside the `{{ define "header" }}` block of `src/frontend/templates/header.html` — alongside the existing currency selector / cart-icon controls (positioned by the `controls` `<div>`). Use existing navbar CSS classes so the layout doesn't break. After this change, the search input must render on home, product detail, cart, checkout, and assistant pages without further per-page edits.

- [ ] T015 [US3] Smoke-test US3 per `specs/001-product-search/quickstart.md` §5: confirm the header search input renders on `/`, `/cart`, and `/product/{id}`; submit `watch` from `/cart` and verify it reaches the same results page; submit `dryer` from a non-Hairdryer product detail page and confirm results show only the Hairdryer (no preferential treatment for the previously-viewed product).

**Checkpoint**: All three user stories independently functional. Spec is fully implemented (modulo Polish below).

---

## Phase N: Polish & Cross-Cutting Concerns

**Purpose**: Observability, full verification pass against spec success criteria, and the SC-008 invariant check that protects the constraints contract.

- [X] T016 [P] Add a structured log line to `searchHandler` in `src/frontend/handlers.go` immediately before template execution: `log.WithField("query", trimmedQuery).WithField("count", len(items)).Info("search")`. Uses the existing logrus logger from `r.Context().Value(ctxKeyLog{}).(logrus.FieldLogger)`, same pattern as `homeHandler` at line 61.

- [ ] T017 [P] Run currency-switching verification per `specs/001-product-search/quickstart.md` §3: switch to JPY on a results page, confirm all result-card prices render with `¥` prefix; switch to GBP, confirm all switch to `£` (no stale `¥`).

- [ ] T018 [P] Run case-insensitivity verification per `specs/001-product-search/quickstart.md` §2: confirm `Watch`, `watch`, `WATCH`, `wAtCh` produce the same result set.

- [ ] T019 [P] Run backend unit-test verification per `specs/001-product-search/quickstart.md` §6: `cd src/productcatalogservice && go test ./...` — confirm `TestSearchProducts` (existing) and `TestSearchProductsNameOnly` (T003) both pass.

- [X] T020 [P] Run SC-008 invariant check per `specs/001-product-search/quickstart.md` §7: confirm `git diff origin/attendee/tim-love...HEAD --name-only` lists zero files matching `^(kubernetes-manifests|kustomize|helm-chart|istio-manifests|terraform|\.github/workflows|cloudbuild\.yaml)`. Run the exact command from the quickstart and expect `PASS`.

- [X] T021 [P] Re-validate `specs/001-product-search/checklists/requirements.md`: re-read each of the 16 checklist items against the now-implemented code and confirm none have regressed. Update the checklist notes section if any item now requires a caveat.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — start immediately.
- **Foundational (Phase 2)**: Depends on Setup completion. Single read-only verification task.
- **User Story 1 (Phase 3)**: Depends on Foundational. Blocks US2 and US3 because both extend US1's files.
- **User Story 2 (Phase 4)**: Depends on US1 — extends `searchHandler` and `search-results.html`.
- **User Story 3 (Phase 5)**: Depends on US1 — moves the form added by US1.
- **Polish (Phase N)**: Depends on US1, US2, US3 — all verification tasks assume the feature is functionally complete.

### User Story Dependencies

- **US1**: No story dependencies. Foundation for everything else.
- **US2**: Soft dependency on US1 (shares files; commits should land after US1's commits). Each story is independently demonstrable: US1 = happy path works; US2 = bad inputs are pretty. You can ship US1 to production without US2 (the experience just has rough edges around empty/oversize queries).
- **US3**: Soft dependency on US1 (moves the form added by US1). Independently demonstrable: search reachable from cart and product-detail pages.

### Within Each User Story

- US1: test (T003) → backend narrowing (T004); meanwhile rpc-client (T005), template (T006), home-page form (T009) can land in parallel. Handler (T007) needs T005 + T006. Route (T008) needs T007. Smoke test (T010) needs everything.
- US2: handler change (T011) and template change (T012) are different files and can run in parallel; smoke test (T013) needs both.
- US3: form move (T014) → smoke test (T015). No parallelism within the story.
- Polish: T016 is a small code edit; T017-T021 are read-only verifications. All can run in parallel.

### Parallel Opportunities

- T005, T006, T009 (within US1): three different files, no inter-dependencies.
- T011, T012 (within US2): two different files.
- T016, T017, T018, T019, T020, T021 (Polish): six tasks, mutually parallel.

---

## Parallel Example: User Story 1

Once T003 (test) is committed and failing, and T004 (backend narrow) is unblocked, the three [P] tasks within US1 can be picked up in parallel by different developers (or batched into a single PR if one developer):

```text
# All three of these touch different files and have no inter-dependencies:
Task T005 [P] [US1]: rpc.go — add searchProducts client wrapper
Task T006 [P] [US1]: templates/search-results.html — new template
Task T009 [P] [US1]: templates/home.html — search input form

# After T005 + T006 complete:
Task T007 [US1]: handlers.go — implement searchHandler  (needs T005's wrapper + T006's template)
Task T008 [US1]: main.go — register /search route       (needs T007's handler)
Task T010 [US1]: smoke test                              (needs all of the above + T004's narrowing)
```

---

## Implementation Strategy

### MVP First (User Story 1 only)

1. Complete Phase 1: Setup (T001).
2. Complete Phase 2: Foundational (T002).
3. Complete Phase 3: User Story 1 (T003–T010).
4. **STOP and VALIDATE**: smoke-test US1 end-to-end. The shopper can search from the home page; happy path works; bad inputs are ugly but not broken.
5. Decide whether to ship the MVP immediately or batch with US2/US3.

### Incremental Delivery

1. Setup + Foundational → unblocks all stories.
2. US1 → ship the MVP (search from home, happy path).
3. US2 → ship error-handling polish (empty / no-match / oversize).
4. US3 → ship cross-page reach (search lives in header).
5. Polish → run the SC-008 invariant check before the final PR merge; add the log line; re-validate the requirements checklist.

### Constraint-Compliance Guardrails

At every commit boundary, before pushing:

```sh
git diff origin/attendee/tim-love...HEAD --name-only | \
  grep -E '^(kubernetes-manifests|kustomize|helm-chart|istio-manifests|terraform|\.github/workflows|cloudbuild\.yaml)' \
  && echo "FAIL — infra files touched, SC-008 violated" \
  || echo "ok"
```

If `FAIL` ever prints, stop and roll back the offending change before continuing. This is the single most important invariant in the constraints block.

---

## Notes

- `[P]` tasks = different files, no cross-task dependencies. Mark conservatively — when in doubt, omit `[P]`.
- `[Story]` label = traceability back to spec user stories (US1 → spec.md §User Story 1, etc.).
- TDD scope is limited to the one backend test (T003) explicitly mandated by `contracts/searchproducts.rpc.md`. The frontend handler is not test-driven; the codebase has no precedent for frontend handler tests and adding the test scaffolding is out of scope for this slice.
- Commit after each task or each logical group. Do **not** force-push or rebase the `001-product-search` branch once it has the CI deployment hook attached.
- Before merging, run T020 — the SC-008 git diff invariant check — and confirm it prints `PASS`. This is the headline constraint from the spec.
