# Quickstart: Product Search

**Plan**: [plan.md](./plan.md) | **Spec**: [spec.md](./spec.md) | **Research**: [research.md](./research.md) | **Data model**: [data-model.md](./data-model.md)

This is the developer's guide for building, running, and verifying the product-search feature. The customer-facing behaviour is defined in [spec.md](./spec.md); this document bridges that spec to the implementation tasks (`tasks.md` will be generated by `/speckit-tasks`).

## Files you will touch

Three files modified, three created. All inside `src/frontend/`.

| File                                                            | Change          | Purpose                                                                                                                              |
|-----------------------------------------------------------------|-----------------|--------------------------------------------------------------------------------------------------------------------------------------|
| `src/frontend/templates/header.html`                            | modify (1 line) | Add one `<link rel="stylesheet" href="…/search.css">` next to the other stylesheet links.                                            |
| `src/frontend/templates/home.html`                              | modify          | Add search input + clear button + empty-state element directly above `<div class="row hot-products-row …">`. Add `data-product-name="{{ .Item.Name }}"` to each `<div class="col-md-4 hot-product-card">`. Add `<script src="…/search.js" defer>` at the end of `{{ define "home" }}`. |
| `src/frontend/static/styles/search.css`                         | **new** (~30 lines) | Layout for `.search-wrapper`, `#search-input`, `#search-clear`, `.search-empty-state`. Sized to not displace existing layout (C-010). |
| `src/frontend/static/js/`                                       | **new** dir     | First JS directory in the repo (mirrors the existing `static/styles/` separation pattern).                                            |
| `src/frontend/static/js/search.js`                              | **new** (~50 lines vanilla JS) | The filter behaviour: on `input` and `keydown` (for Esc), iterate `.hot-product-card`, toggle `style.display`, toggle empty-state, toggle clear button. |
| `src/frontend/home_test.go`                                     | **new** (~80 lines Go) | Drives `homeHandler` via `httptest` and asserts the markup contract from `data-model.md`.                                            |

Nothing else in the repo is touched. In particular, no file under `src/productcatalogservice/`, `kubernetes-manifests/`, `helm-chart/`, `kustomize/`, `terraform/`, `istio-manifests/`, `release/`, `protos/`, `cloudbuild.yaml`, `skaffold.yaml`, or any `Dockerfile` is modified.

## Build & run locally

The repo uses skaffold. Two options:

### Option A — full skaffold dev (slow, but everything wired up)

```bash
skaffold dev
```

Then open `http://localhost:8080/` (or whatever the skaffold port-forward reports).

### Option B — frontend only (fastest iteration once dependencies are up)

Bring backend services up first via skaffold or `docker compose` per the existing repo docs, then:

```bash
cd src/frontend
go build .
PRODUCT_CATALOG_SERVICE_ADDR=localhost:3550 \
CURRENCY_SERVICE_ADDR=localhost:7000 \
CART_SERVICE_ADDR=localhost:7070 \
RECOMMENDATION_SERVICE_ADDR=localhost:8080 \
SHIPPING_SERVICE_ADDR=localhost:50051 \
CHECKOUT_SERVICE_ADDR=localhost:5050 \
AD_SERVICE_ADDR=localhost:9555 \
SHOPPING_ASSISTANT_SERVICE_ADDR=localhost:9090 \
PORT=8080 \
./frontend
```

(Adjust the `_SERVICE_ADDR` env vars to wherever your backends are listening.)

## Run the Go test

```bash
cd src/frontend
go test ./...
```

This must pass `home_test.go`'s assertions on the markup contract.

## Manual verification — the 12 checks

Each row maps to a numbered acceptance scenario or success criterion in the spec. Walk these in DevTools open.

| #  | Spec ref                | Steps                                                                                                  | Expected outcome                                                                                       |
|----|-------------------------|--------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------|
| 1  | US1 #1 / SC-002         | Load `/`. Type `watch` in the search input.                                                            | Exactly the Watch card visible.                                                                       |
| 2  | US1 #2                  | Continue from #1. Delete the text.                                                                     | All 9 cards visible, in original order.                                                               |
| 3  | US1 #3                  | Type `WATCH`.                                                                                          | Same one card as #1 — case ignored.                                                                   |
| 4  | US1 #4                  | Type `ass`.                                                                                            | Exactly Sunglasses + Bamboo Glass Jar visible.                                                        |
| 5  | US1 #5                  | Type `&`.                                                                                              | Exactly Salt & Pepper Shakers visible.                                                                |
| 6  | US1 #6 / SC-004 / C-013 | Open DevTools → Network. Type, delete, retype anything.                                                | Zero new network requests fire during any typing.                                                     |
| 7  | US1 #7                  | Type a sequence of characters very rapidly (paste a 10-char string).                                   | After paste settles, visible grid matches the final value only — no stale cards left over.            |
| 8  | US2 #1 / SC-005         | Type `zzzzz`.                                                                                          | "No products match your search." replaces the grid; no cards visible; aria-live announcement fires.   |
| 9  | US2 #5 / SC-006 / FR-009 | Type or paste `<script>alert(1)</script>`.                                                            | No `alert` dialog. Elements panel: no live `<script>` tag in the search/empty-state region.            |
| 10 | US3 #2 / SC-008         | Type `watch`. Click the × button inside the input.                                                     | Input empties; full grid returns.                                                                     |
| 11 | US3 #3 / FR-008         | Type `watch`. Press Esc.                                                                               | Same as #10.                                                                                          |
| 12 | SC-010 / FR-010 / C-012 | DevTools → Console. Note `history.length` and `window.location.href`. Type/edit/clear several times. Re-check. | `history.length` unchanged. URL unchanged. No new dialog / modal element appears.                     |

Two extra spot-checks that don't map cleanly to a numbered scenario but are still part of the contract:

| #   | Spec ref       | Steps                                                                                                                                                  | Expected outcome                                                                                  |
|-----|----------------|--------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------|
| L1  | SC-007 / C-010 | Reload the page with the search input empty. Compare the rendered grid (Elements panel) against the previous-feature-off baseline.                       | Same set of cards, same order, same per-card content (image, name, price). Header / footer / currency selector / Hot Products heading all unchanged in position. |
| L2  | SC-009 / C-011 | Open an incognito window with no prior cookies. Load `/`. Walk checks 1, 6, 8, 10 above.                                                                | Same behaviour as in a logged-in session. No login prompt appears.                                |

## Constraint-compliance self-check

Before you open the merge into `attendee/jenny-svennefelt`, walk the constraints list. Your diff must NOT touch:

- any directory under `src/` other than `src/frontend/`
- any file under `kubernetes-manifests/`, `helm-chart/`, `kustomize/`, `terraform/`, `istio-manifests/`, `release/`
- any `Dockerfile`, `cloudbuild.yaml`, or `skaffold.yaml`
- any `.proto` file or `genproto.sh`
- `src/productcatalogservice/products.json`
- `go.mod` / `go.sum` in any service (no new Go deps)

If `git diff main...001-product-search --stat` shows files only under `src/frontend/templates/*.html`, `src/frontend/static/**`, and `src/frontend/*_test.go`, you are inside the lines.

## Merge back to `attendee/jenny-svennefelt`

Per C-007, this dev branch (`001-product-search`) merges back to `attendee/jenny-svennefelt` before CI deploys. Suggested:

```bash
git checkout attendee/jenny-svennefelt
git merge --ff-only 001-product-search
git push origin attendee/jenny-svennefelt
```

If a fast-forward isn't clean (the attendee branch moved on independently), prefer a merge commit (`git merge 001-product-search`) over a rebase — the spec branch is shared via `origin/001-browser-product-search`-style remotes for course bookkeeping, and rebasing would rewrite shared history.
